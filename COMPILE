To compile daisy-player, be sure the following extra packages are installed:

   libncursesw5-dev
   libidn11-dev
   libsox-dev
   libxml2-dev

Give the following command:

   $ make

run the script install.sh, as user root, to install daisy-player onto
your system:

   # ./install.sh
