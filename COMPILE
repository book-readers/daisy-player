To compile daisy-player, be sure the following packages are installed:

   libncursesw5-dev

Give the following command:

   $ make

run the script install.sh, as user root, to install daisy-player on
to your system:

   $ ./install.sh
